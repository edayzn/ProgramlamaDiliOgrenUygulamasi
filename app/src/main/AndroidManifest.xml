<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.programlamadiliogren">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AdminSoruCevapActivity"></activity>
        <activity android:name=".IcerikActivity" />
        <activity android:name=".AdminIcerikActivity" />
        <activity android:name=".KonularYeniActivity" />
        <activity android:name=".AdminAltbaslikActivity" />
        <activity android:name=".AltbaslikActivity" />
        <activity android:name=".AdminKonularActivity" />
        <activity android:name=".KonularActivity" />
        <activity android:name=".AdminHomeActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>